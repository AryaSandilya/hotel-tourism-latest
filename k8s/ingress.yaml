apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tourism-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tourism-service
                port:
                  number: 80